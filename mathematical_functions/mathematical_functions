import math - импорт модуля математических функций

abs(-5) - вычисление модуля числа
min(1, 2, 3...) - вычисляет минимальное среди переданных ей чисел
max(1, 2, 3...) - вычисляет максимальное среди переданных ей чисел
pow(6, 2) - возведение чисел в определенную степень
round(0.5) - округление к ближайшему целому
! round(0.4568734, 2) - округление до сотых
! round(0.4568734, 3) - округление до тысячных и т.д.
! round(0.4568734, -1) - округление до десятков
! round(0.4568734, -2) - округление до сотен

MATH:
math.ceil(5.2) - округление числа в наибольшую сторону
math.floor(5.99) - округление числа в наименьшую сторону
math.factorial(6) - вычисляет факториал числа
math.trunc(5.67) - отбрасывается дробная часть / int(7.8394)
math.log2(4)/log10(1000) - вычисление логарифма
math.sqrt(27) - вычисление квадратного корня
math.sin/cos
math.pi
math.e
